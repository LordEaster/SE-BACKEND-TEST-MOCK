/**
 * โจทย์ backend ข้อที่ 14
 * เขียนฟังก์ชันเพื่อตรวจสอบว่าอาร์เรย์ที่กำหนดให้นั้นเรียงจากน้อยไปมากหรือไม่
 * 
 * ตัวอย่าง
 * array = [1, 2, 3, 4, 5]
 * 
 * ผลลัพธ์: true
 */

process.stdin.on('data', (data) => {
    const input = JSON.parse(data.toString());

    function exam14(arr) {
        //  แก้ไขโค้ดตรงนี้
    }

    console.log(exam14(input.arr));
});